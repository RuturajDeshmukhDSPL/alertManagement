NODE_ENV=development
PORT=3000

# MySQL connection — matches the `alertManagement` database from your SQL script
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=alertManagement
DB_USER=root
DB_PASSWORD=Ruturaj@123

# Allow the Angular dev server to call this API
CORS_ORIGIN=http://localhost:4200
